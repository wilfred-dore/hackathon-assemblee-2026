# Secrets — JAMAIS commité
.env
.env.*
!.env.example

# Python / uv
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
*.egg-info/
uv.lock

# Node (optionnel — schémas Tricoteuses)
node_modules/

# OS / éditeurs
.DS_Store
.idea/
.vscode/

# Données lourdes déposées localement
data/fixtures/*
!data/fixtures/README.md
!data/fixtures/.gitkeep

# Sorties générées (SchemaSpy HTML brut, volumineux)
out/

# Build statique GitHub Pages de l'app — généré par poc/build_static.py (François)
/site/

# DemoDSL — venv + artefacts de génération vidéo
.demodsl/
demo/output/
demo/_template.yaml
